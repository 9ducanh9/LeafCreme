-r requirements.txt

# Dev/CI only — kept out of the production Docker image on purpose
# (smaller image, smaller attack surface; test tooling has no business
# running in production). httpx is already in requirements.txt (used by
# MoMo integration) so it's not repeated here.
pytest>=8.0
pytest-asyncio>=0.23
# httpx is already included via requirements.txt (Starlette TestClient dependency).
# (intentionally not duplicated here)
ruff>=0.5
